#include "main.h"
/**
 * _abs - computes the absolute value of integer
 * @n: the int to be check
 * Return: thw absolute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
